import TestDoublesRuntimeMetadata

// Keep execution sources focused on ABI work while explicitly selecting the
// package's descriptor model over Echo's unrelated context descriptor type.
package typealias MethodDescriptor = TestDoublesRuntimeMetadata.MethodDescriptor
